---
title: Indian Railway Stations API
description: Comprehensive API documentation for fetching and managing Indian railway station data using the JSONly API.
icon: TrainFront
---

# 🇮🇳 🚆 Indian Railway Stations API

Welcome to the **Indian Railway Stations API**. This API allows you to fetch and manage detailed data about railway stations across India — including station codes, names, and associated cities.

---

## 🔗 Base URL

```bash
https://jsonly.vercel.app/api/india/railway-stations
```

---

## ⚙️ Query Parameters (GET)

<TypeTable
  type={{
    station: {
      description: 'Search by station name, code, or city (fuzzy).',
      type: 'string',
      default: 'None'
    },
    limit: {
      description: 'Limits how many stations are returned.',
      type: 'number',
      default: '10'
    },
    offset: {
      description: 'Skips the specified number of stations.',
      type: 'number',
      default: '0'
    }
  }}
/>

---

<Steps>

<Step>
### Search Stations

**Endpoint:**
```bash
GET /api/india/railway-stations?station=NDLS
```

#### Output Example
```json
{
  "success": true,
  "total": 1,
  "data": [
    {
      "stnCode": "NDLS",
      "stnName": "New Delhi",
      "stnCity": "New Delhi"
    }
  ]
}
```
</Step>

<Step>
### Pagination

**Endpoint:**
```bash
GET /api/india/railway-stations?limit=5&offset=10
```

<Callout type="info">
The default `limit` is 10. You can paginate through thousands of stations using `offset`.
</Callout>
</Step>

</Steps>

---

## 🛠️ Mutations (Full CRUD)

<Callout type="warn">
Mutations are currently **in-memory** and will revert to the original state upon server restart.
</Callout>

### 1. Add a Station (POST)

Register a new railway station.

```bash
POST /api/india/railway-stations
Content-Type: application/json

{
  "stnCode": "TEST",
  "stnName": "Test Station",
  "stnCity": "Test City"
}
```

### 2. Update a Station (PUT/PATCH)

Update station details by its code.

```bash
PATCH /api/india/railway-stations
Content-Type: application/json

{
  "stnCode": "TEST",
  "stnName": "Updated Test Station",
  "stnCity": "Updated Test City"
}
```

### 3. Delete a Station (DELETE)

Remove a station from the database.

```bash
DELETE /api/india/railway-stations?stnCode=TEST
```

---

<Callout emoji="🎫">
Building a travel app? Combine this with our [India Cities API](/docs/india/indian-cities-api) for a complete experience.
</Callout>